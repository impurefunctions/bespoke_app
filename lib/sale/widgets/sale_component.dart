import 'package:flutter/material.dart';

class SaleComponent extends StatelessWidget {
  const SaleComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}