import 'package:flutter/material.dart';

class TransactionsComponent extends StatelessWidget {
  const TransactionsComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}